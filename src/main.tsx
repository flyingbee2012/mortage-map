import React from "react";
import ReactDOM from "react-dom/client";
import MortgageMap from "./components/MortgageMap";
import "./index.css";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <MortgageMap />
  </React.StrictMode>,
);
